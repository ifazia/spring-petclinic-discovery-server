spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: docker

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
